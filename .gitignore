# Secrets — match any .env anywhere in the tree
**/.env
**/.env.*
!**/.env.example

# Python caches
__pycache__/
*.pyc
.venv/

# Backend runtime artifacts
backend/chroma_db/
backend/lib/
backend/storage/
backend/kg.html

# Claude Code machine-local config (skills/ and commands/ ARE tracked — they're project-wide playbooks)
.claude/settings.local.json
.claude/transcripts/
